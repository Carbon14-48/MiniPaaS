apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployer-service
  namespace: minipaas
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deployer-service
  template:
    metadata:
      labels:
        app: deployer-service
    spec:
      imagePullSecrets:
      - name: ghcr-secret
      containers:
      - name: deployer-service
        image: ghcr.io/Carbon14-48/minipaas-deployer-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: minipaas-config
        - secretRef:
            name: minipaas-secret
        volumeMounts:
        - name: docker-socket
          mountPath: /var/run/docker.sock
        livenessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          tcpSocket:
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      volumes:
      - name: docker-socket
        hostPath:
          path: /var/run/docker.sock
          type: Socket
